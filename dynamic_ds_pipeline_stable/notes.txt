I found a thread from three days ago describing the exact same issue — remove operations work initially but then hang or timeout after repeated add/remove cycles, particularly under load. An NVIDIA engineer is still investigating and hasn't provided a resolution yet, which confirms this is a known, open issue in DS 8.0 rather than user error.
The pattern is clear: the instability worsens with higher resolution and GPU load, stays more stable at lower resolutions like 640x360, and there's a quirk where source_id changes after re-adding a source even though camera_id remains constant. This appears to be part of a longer-standing class of issues around streammux pad release causing pipeline hangs during repeated add/remove cycles.
So the reality is you've hit a documented DeepStream 8.0 limitation that NVIDIA is actively aware of. But I can suggest some mitigations to improve stability — starting with removing the file-loop since constantly seeking sources and removing them mid-seek is likely triggering deadlocks, which the reference pipeline doesn't do.
Beyond that, I'd apply the stability properties from the official gst-launch example like batched-push-timeout and drop-pipeline-eos, reduce the resolution and load to something like 640x360 since the thread explicitly shows lower res is more stable, add serialization guards so add and remove operations don't overlap, and increase the client timeout since the current 10-second read timeout might be cutting off operations before they complete internally.
For testing, I should avoid immediately re-using the same camera_id after removal since the source_id reshuffling on re-add seems to be a trigger point. The real situation here is that this is a known DeepStream 8.0 issue without a complete fix yet—I can offer mitigations that improve stability, but I can't guarantee rock-solid repeated remove operations because NVIDIA hasn't resolved it themselves. I should be honest with the user about this being a documented issue confirmed by recent forum threads with identical symptoms. Even with mitigations in place, the core issue—repeated remove operations triggering nvstreammux pad release hangs—is a fundamental SDK limitation on DS 8.0 that won't fully disappear. File-loop removal helps reduce the problem's frequency, but the user's RTSP-based failure shows the hang occurs regardless of input source, meaning it's baked into the remove logic itself. I should be direct that this is an open issue they may need to work around or wait for NVIDIA to address through a version update. Without file-loop, file sources will EOS after ~20 seconds with sync=1, making them removable only within that window—drop-pipeline-eos=1 keeps the pipeline alive but the source itself disappears after playback ends. Live RTSP sources avoid this entirely since they don't have a finite duration, making them the most reliable for testing removes without triggering the deadlock that file-loop accelerates.
For a realistic simulation that mirrors the forum's more stable approach, I should lower the resolution to 640x360, remove file-loop, apply the stability properties, and perform removes within the source's 20-second lifetime—or ideally set up a local RTSP test stream to eliminate the timing constraint altogether.
I'll give the user the full picture: this is a documented DS 8.0 issue with an open NVIDIA thread, so it's not their code. Then I'll provide the concrete mitigations that help—dropping file-loop, adding the batched-push-timeout and drop-pipeline-eos properties, lowering resolution, and increasing client timeout—plus mention the RTSP option as the most stable path if they want to invest the setup time.
Now I'm updating the ds.py with the tuned properties on nvmultiurisrcbin: setting resolution to 640x360, adding the batched-push-timeout and drop-pipeline-eos flags, and deciding whether to include live-source (I'll note it cautiously since file sources can behave differently than live streams).
Writing updated ds.py... The core issue is that file sources either end after ~20 seconds (making removal fail) or loop indefinitely (which accelerates the deadlock). This creates a genuine trade-off: RTSP sources would give a stable demo but require extra setup, while file sources with looping are convenient but prone to hangs. I'm weighing whether to recommend pivoting toward RTSP or accepting occasional hangs with mitigated settings and lower load.
Actually, the most helpful approach is to be direct about this being a known SDK issue on DS 8.0, set proper expectations so they don't waste time debugging their own code, and then provide the best practical workaround: disable file-loop, apply the pipeline-eos and batched-push-timeout settings, reduce resolution to 640x360, and use a gentler cadence with longer timeouts in the test. I'll also mention that RTSP sources are more stable per the forum discussion.
For the test itself, I'm thinking a simpler pattern—add each source, wait briefly, then remove it while still active, with a 30-second client timeout and a small settle delay between operations. This low-load, remove-while-active pattern is what the thread indicates is more stable. Without file-loop enabled and with this sequential approach, the timing should be more predictable than the multi-phase batch test. keeps each source alive when removed and avoids piling up. Given this is a known issue, I don't want to over-engineer the test — I'll provide the mitigated ds.py with stability improvements and a more pragmatic test approach, then set clear expectations about the limitations. I should also bump the client read timeout to around 30 seconds so a slow remove operation doesn't get misreported as a hang. On the file-loop question, the thread shows RTSP sources hang too, so looping isn't the root cause, but the seeking it introduces does add risk — I'll drop it from the mitigated version and explain the reasoning while noting that RTSP is the more stable option according to NVIDIA's testing.
Now I'm structuring the response to be honest about the known bug upfront. I'll lead with the diagnosis, then walk through the mitigations with the updated ds.py configuration, explain the gentler test approach, and set expectations around what's still uncertain. For the ds.py settings, I'm using 640x360 resolution, a batched-push-timeout of 33333, drop-pipeline-eos enabled, no file-loop, and sync mode on. I need to verify whether removing file-loop reintroduces the source EOS and remove failure problem, but with drop-pipeline-eos=1 that should be handled.
For the test, I'll pair add and remove operations with a 3-5 second gap between them to keep sources alive during their lifecycle, then provide both the updated configuration and a revised test with a 30-second timeout and a churn-style cadence rather than bulk operations.
Writing the updated code... Setting up the GStreamer pipeline...
I'm initializing the pipeline and configuring the multi-URI source with network settings, batch sizing, and frame dimensions, then disabling per-source EOS handling to keep the pipeline stable. Now I'm adding the tiler element to handle multiple streams.
Configuring pipeline elements...
I'm setting up the tiler for a 2x2 grid layout, then chaining together the video converter, on-screen display, and fake sink elements. After adding everything to the pipeline and linking them in sequence, I'm creating a GLib main loop with bus message handling to monitor the pipeline state. Once I start playback and print the REST endpoint, the loop runs until interrupted, then cleanly shuts down the pipeline.
I should double-check those property names like batched-push-timeout and drop-pipeline-eos against the actual GStreamer documentation to make sure they're correct. For the test, I'll increase the timeout and use a pattern of adding and removing the camera source repeatedly — adding it, waiting a few seconds, removing it, waiting a bit longer, then repeating that cycle.
Looking at the actual file behavior: at 640x360 with sync=1, the video plays in real-time, so a ~20-30 second file takes about that long to process. Within a 3-5 second add-remove window, the source will definitely still be active when I remove it. I'm restructuring the test so sources are always alive during removal — ramping up to 4 concurrent cameras with 2-second gaps between additions, then removing each one within its own window.
Actually, the safest approach is to churn single sources: add one camera, wait 4 seconds, remove it, wait 2 seconds, then repeat for multiple cameras and a few re-add cycles. This keeps only 1-2 sources active at a time, minimizing load and ensuring stability. I'll also add a brief phase with up to 3 concurrent sources to test that scenario, keeping the timeout at 30 seconds.
Writing test code... Writing test scenarios...
Now I'm planning how to structure the response—I should lead with transparency about the known bug, then walk through the mitigations and relevant files, and set clear expectations. I'll reference the forum discussion from a few days ago where NVIDIA acknowledged the issue in DS 8.0 with the same container setup, noting they're still investigating. The key insight is that the problem scales with system load, so I need to emphasize that context when explaining the workarounds. I'm structuring the response with the diagnosis, then the updated ds.py code, followed by the test expectations to validate the fix.Show lessDoneNow I understand what you're up against, and it's worth saying clearly: this is a known DeepStream 8.0 bug, not a mistake in your code. There's a forum thread posted three days ago, on the same container image, describing your exact symptom — remove works once or several times, then hangs/times out after repeated add/remove/re-add cycles, after which the runtime becomes unresponsive and the remaining camera can be affected. As of now an NVIDIA engineer is still investigating and there's no posted fix. So the read-timeouts you saw on cam3/cam4 are the pipeline deadlocking during source cleanup, exactly as reported. GitHubGitHub
A few things genuinely help, drawn from that thread and NVIDIA's reference command:
The single most useful finding: it's load-dependent. The reporter found remove is more stable at lower resolution such as 640x360 and lower inference frequency, and more likely to hang under 1080p and high GPU utilization. Your pipeline is running at 1280×720 — dropping to 640×360 meaningfully reduces the deadlock rate. GitHub
Second, file-loop was making it worse. A looping source is constantly seeking back to the start, and removing one mid-seek is an extra deadlock path on top of the base bug. NVIDIA's reference pipeline doesn't loop — it sets drop-pipeline-eos=1 so the pipeline survives a source ending, instead. Your pipeline was also missing batched-push-timeout and drop-pipeline-eos that the reference command sets.
